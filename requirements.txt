# Runtime dependencies for Webgore
#
# On Windows, install the GTK3 runtime and WebKit2GTK via MSYS2:
#
#   pacman -S mingw-w64-x86_64-python-gobject \
#             mingw-w64-x86_64-webkitgtk3
#
# Then install this package's Python extras inside the MSYS2 shell:
#
#   pip install -r requirements.txt
#
# PyGObject binds the GLib/GTK/WebKit2 C libraries to Python.
PyGObject>=3.44.0
